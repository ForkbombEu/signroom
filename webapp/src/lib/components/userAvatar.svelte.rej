diff a/webapp/src/lib/components/userAvatar.svelte b/webapp/src/lib/components/userAvatar.svelte	(rejected hunks)
@@ -6,16 +6,16 @@
 	const sizeToNumber = {
 		xs: 24, // to be defined
 		sm: 32, // to be defined
-		md: 40, 
-		lg: 80, 
+		md: 40,
+		lg: 80,
 		xl: 98 // to be defined
 	};
-    //@ts-ignore
-    const src = pb.files.getUrl($currentUser, $currentUser?.avatar);
+	//@ts-ignore
+	$: src = pb.files.getUrl($currentUser, $currentUser?.avatar);
 </script>
 
 {#if $currentUser?.avatar}
 	<Avatar {size} {src} />
 {:else}
-	<BoringAvatar variant="beam" size={sizeToNumber[size || "md"]} name={$currentUser?.name} />
+	<BoringAvatar variant="beam" size={sizeToNumber[size || 'md']} name={$currentUser?.name} />
 {/if}
