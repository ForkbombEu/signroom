diff a/webapp/src/lib/utils/strings.ts b/webapp/src/lib/utils/strings.ts	(rejected hunks)
@@ -1 +1,2 @@
-export const capitalizeFirstLetter = (text:string) => text.charAt(0).toUpperCase() + text.slice(1)
\ No newline at end of file
+export const capitalizeFirstLetter = (text: string) => text.charAt(0).toUpperCase() + text.slice(1);
+export const c = capitalizeFirstLetter;
